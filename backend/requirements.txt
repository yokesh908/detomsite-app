fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6
httpx==0.25.0
python-dotenv==1.0.0
PyJWT==2.8.0
email-validator==2.1.0
pillow==10.1.0
cloudinary==1.36.0
sentry-sdk==1.38.0
prometheus-client==0.19.0
cryptography==41.0.7
python-slugify==8.0.1
razorpay==2.0.1
psycopg2-binary==2.9.9
pywebpush==1.14.1
# Shared read cache (app/core/redis_cache.py). Two transports, both supported:
#   * Upstash / Vercel KV REST — KV_REST_API_URL + KV_REST_API_TOKEN, spoken
#     over httpx (already a dependency above).
#   * Raw RESP — REDIS_URL=rediss://… for Render Key Value / Redis Cloud /
#     Railway / self-hosted. That transport needs the client below.
# The import is guarded and every call is best-effort, so an unreachable or
# unconfigured Redis can never break a request: reads fall back to the Postgres
# app_cache table. Leave the env vars empty and everything still works.
redis>=5.0
setuptools>=70
