fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.6
httpx==0.25.0
python-dotenv==1.0.0
PyJWT==2.8.0
email-validator==2.1.0
pillow==10.1.0
cloudinary==1.36.0
sentry-sdk==1.38.0
prometheus-client==0.19.0
cryptography==41.0.7
python-slugify==8.0.1
razorpay==2.0.1
psycopg2-binary==2.9.9
pywebpush==1.14.1
# The shared read cache speaks the Upstash/Vercel KV REST API over httpx (already
# a dependency), so no Redis client is required there. Install `redis` only when
# using a plain REDIS_URL (Redis Cloud / Railway / Render Key Value / self-hosted):
#   pip install "redis>=5.0"
# Leave it out and the cache transparently falls back to the Postgres app_cache
# table; nothing breaks either way (see app/core/redis_cache.py).
setuptools>=70
